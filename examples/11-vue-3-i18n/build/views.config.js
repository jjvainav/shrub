/** Defines assets used by the build process when building/copying views to the output folder. */
module.exports = {
    views: [{ name: "main", template: "./main/index.html", entry: "./main/entry.js" }]
};