{
    "name": "@examples/01-hello-world",
    "version": "1.0.0",
    "private": true,
    "main": "dist/index.js",
    "typings": "dist/index.d.ts",
    "scripts": {
        "build": "concurrently \"tsc -b\" \"yarn run copy-vue\"",
        "clean": "rimraf ./dist && rimraf tsconfig.tsbuildinfo",
        "copy-vue": "copyfiles -u 1 ./src/components/**/*.vue dist"
    },
    "dependencies": {
        "@shrub/module": "^0.1.16",
        "@shrub/service-collection": "^0.1.16",
        "@shrub/vue-core": "^0.1.16",
        "vue": "^2.6.10"
    },
    "devDependencies": {
        "copyfiles": "^2.1.1",
        "concurrently": "^4.1.2"
    }
}